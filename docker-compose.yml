services:
  watcher:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATA_FILE: /app/data/db.json
    volumes:
      - watcher-data:/app/data
    restart: unless-stopped

volumes:
  watcher-data:
